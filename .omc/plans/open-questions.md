# Open Questions

## apohara-codesearch v0.1 - 2026-06-04

No open questions remain. All three former open questions were CLOSED as decisions in consensus iteration 2 (see "Resolved Decisions" + "Changelog (consensus iteration 2)" + ADR in `.omc/plans/apohara-codesearch-v0.1.md`).

### Closed decisions (moved from open questions)
- [x] camelCase indexing depth → `body_tokens` shadow column split by shared `code_tokens()` (snake+camel); FTS5 indexes `body_tokens`, bidirectional `parse_string ↔ parseString`. (P0-2 / Step 2 / Choice B1; AC-12 rewritten.)
- [x] Symbol end-line source → add `parse_source_spans(content, lang)` capturing `node.end_position().row + 1`. Inferring from next-symbol start rejected (breaks nested/non-contiguous symbols). (Step 1 / R-8.)
- [x] Window-chunk constants → 60-line window, 15-line overlap, exposed as tunable `const`. (Step 1.)

## apohara-codesearch v0.8 (EmbeddingGemma opt-in embedder) - 2026-06-06 (updated it.2)

- [x] Identity Gate (Decision (h)) — **RESOLVED 2026-06-11** (AskUserQuestion batch + Pablo sign-off, plan `.omc/plans/apohara-codesearch-3frentes.md` iter 2). Decision: **A2 (candle puro sí o sí)** — `ort`/`onnxruntime`/`tch` rejected as deps (even behind a feature flag) to preserve the wedge. The opt-in embedder must compile to the default binary without any native dependencies. Implication: v0.3.0 ships with NO opt-in embedder default; v0.4.0 will measure a code-trained embedder against the candle-pure path before committing Story 3.
- [ ] AC4 scope (Decision (g), it.2 Critic C1) — G1 (RECOMMENDED: degrade the S2 recovery gate to a scoped follow-up; the CSN-NL harness has NO identifier-query slice so it cannot measure AC4; keep v0.8 on the measurable NL lift Q-A) vs G2 (add an identifier/symbol-name query slice to v0.8 to actually verdict AC4 — scope creep). — Pablo chooses. v0.8 makes NO "closes AC4" claim from CSN-NL numbers.
- [ ] Non-gated model mirror to standardize on — `onnx-community/embeddinggemma-300m-ONNX` (if A1/ONNX) vs `unsloth/embeddinggemma-300m-GGUF` (if A2/candle). Depends on the Identity Gate. Official `google/embeddinggemma-300m` is GATED (401).
- [ ] Default Matryoshka dim — 768d (quality, MTEB-Code 68.76, 3 KiB/chunk) vs 256d (66.74, 1 KiB/chunk). Resolved by Story 1's measurement, but Pablo may want a footprint preference pinned in advance.
- [ ] (3a only, it.2 nice-to-have) `ort` raw vs `fastembed` v5 as the integration surface — pin at the Identity-Gate handoff before coding Story 3.

## apohara-codesearch roadmap - 2026-06-05

Decisions deferred to Pablo or to a later phase (see `.omc/plans/apohara-codesearch-roadmap.md`). The three contested architecture decisions are RESOLVED in the plan (ABSORB / synthetic-labeled corpus / fixed-size split) and are NOT open.

- [ ] Push `v0.1.0` tag → PUSH-GATED — only Pablo's `git push origin main --follow-tags` triggers the 5-binary GitHub Release; ralph prepares the tag + CHANGELOG but cannot push.
- [ ] Apohara-Catalyst repo visibility for the NOTICE/README link → HUMAN-ONLY — Pablo decides whether to keep linking the (possibly private) Apohara repo after the ABSORB rewrite drops the "soft-fork/upstream" framing.
- [ ] Claude Code plugin marketplace submission → HUMAN-ONLY — official `claude-plugins-official` is curated by Anthropic with NO application process; community submission (`claude-community`) needs a PUBLIC repo + Pablo submitting via claude.ai/settings/plugins/submit. ralph only runs `claude plugin validate` locally.
- [ ] Phase 4 GGUF embedder → confirm a pure-Rust, offline (no-network) GGUF inference crate exists BEFORE committing the opt-in embedder; if none qualifies, the embedder pluggability ships but the GGUF backend is deferred.
- [ ] Phase 3 external benchmark → real-OSS/real-embedding recall comparison is a one-off offline run pasted into BENCHMARK.md (corpus NOT vendored); decide which OSS repo + which real-embedding tool at that time.
- [ ] CI workflow effect → PUSH-GATED — `ci.yml` is authored locally (EXEC-NOW) but only runs on PRs once pushed.
- [ ] Chunk-size caps (`MAX_CHUNK_LINES=200` / `8 KiB`) → UNTUNED initial defaults (iter 2 / P1-7); revisit once the Phase-1.5 benchmark exists (recall/MRR over varied chunk sizes is the tuning signal). — Why it matters: the defaults are conservative guesses, not measured optima.
- [ ] Phase 1.5 label ordering → labels MUST be authored AFTER 1b/1c land (P0-3), and `.py`/`.go` labels re-authored when Phase 2 lands (P2-11). — Why it matters: labeling against pre-1b chunk ranges measures recall against dead ids (fake regression blamed on retrieval).

## apohara-codesearch v0.5 - 2026-06-05

- [ ] OQ-1: Does `claude plugin validate` expect `marketplace.json` under `.claude-plugin/` instead of repo root? — Determines whether item #5 relocates the file; resolved by running validate.
- [ ] OQ-2: Exact Go repo for the #3 large-OSS soak (must clear 100k Go LOC, permissive license, clone fast offline; candidates: hugo, cosmos-sdk, k8s subtree) — pick at execution time.
- [x] OQ-3 (1b LANDED + measured, US-1B 2026-06-05): `GgufEmbedder` now runs a REAL candle `BertModel` forward-pass (tokenize → forward → attention-masked mean-pool → L2-normalize) over a user-supplied local safetensors checkpoint via `VarBuilder::from_mmaped_safetensors` — std::fs only, no network, no hf-hub. candle-core/candle-nn/candle-transformers/tokenizers are OPTIONAL, `gguf-embed`-gated; `cargo tree -e normal` proves 0 ML crates in the DEFAULT build (enabling `--features gguf-embed` pulls them in — that is the opt-in). Tested: paraphrase cos > unrelated cos, L2-normalized, deterministic, missing-model falls back to feature-hash. **MEASURED VERDICT — a natural-language model does NOT lift code search:** `all-MiniLM-L6-v2` made vector-only recall@5 WORSE on the synthetic corpus (0.083 feature-hash → 0.000 MiniLM), hybrid 0.458 → 0.417, because MiniLM embeds English sentences, not code identifiers, so its neighbors are noise for code queries. The plan said "prove OR disprove" — this DISPROVES a generic NL model. FOLLOW-UP (new OQ): try a CODE-trained embedder (nomic-embed-code / CodeBERT / jina-code class, dim must match or re-DDL chunks_vec); the pluggability + forward-pass infra is done and tested, so it's a model swap. The candle build (~100 deps) is the heaviest in the repo but compiles fine on the 48GB box.
- [ ] OQ-4: If item #4's chunk-cap sweep shows current caps (`MAX_CHUNK_LINES=200` / `MAX_CHUNK_BYTES=8192`) are already optimal, confirm "no change + documented as measured-optimal" is an accepted outcome — confirm with Pablo at #4 time.
- [x] OQ-5 (RESOLVED 2026-06-05, US-O5): The global chunk caps were tuned in v0.5 on Rust+Go only; now validated on TypeScript (`6fbce89` src/) + Python (django `a2348c8`) hand-labeled slices. Result: the 200/8192 default holds across all 4 languages — a `300×16384` spot-check left TS recall unchanged but REGRESSED django (BM25 recall@10 0.800→0.700, MRR 0.3244→0.3067), so larger caps don't help and hurt Python. Per-language behavior matches Rust+Go (feature-hash vector near-noise, hybrid ≤ BM25). See BENCHMARK.md "Per-language cap validation (US-O5 / OQ-5)" + chunker.rs:52/:65 doc comments (updated from "unmeasured"). The vector column is the real lever — deferred to 1b (OQ-3).
- [ ] Release gating (v0.5): cut a `v0.5.0` git tag ONLY if #4 changes a cap (the one user-visible behavior change); tag/push is a GitHub remote op requiring Pablo's explicit OK per his git policy. Marketplace submission stays human-only. — Why it matters: most v0.5 items are docs/measurement/hardening and warrant no release. When tagging, bump FIVE version sites in lockstep: `crates/apohara-indexer/Cargo.toml:5`, `crates/apohara-codesearch/Cargo.toml:5`, `.claude-plugin/plugin.json:5`, `marketplace.json:13`, `npm/package.json:3`.
## apohara-codesearch v0.7 - 2026-06-06
- [x] Story 3 (RESOLVED it.2): cargo-dist 0.32 SÍ soporta attestations nativas — `github-attestations = true` en `[workspace.metadata.dist]` (desde v0.16, PR #1012). Se elimina el hand-edit a release.yml y el lint anti-overwrite; el guard correcto es `dist generate --check` (drift config↔workflow). Queda solo decidir `github-attestations-phase`/`filters`.
- [x] Story 4 (RESOLVED it.2, Decision E1): el path→index map vive en un JSON sidecar bajo config dir (XDG, ~/.config/apohara-codesearch/registry.json), NO en una DB SQLite central compartida. Preserva "one repo per DB"; evita contención cross-process (los `build_locks` de server.rs:99 son process-level). `repo_id` se agrega igual a `files` (PK compuesta). E2 (DB central) rechazado.
- [x] Story 4 (RESOLVED it.2, Decision D corregida): la migración NO es additive — `files(path PRIMARY KEY)` colisiona en multi-repo y SQLite no puede ALTER ADD PRIMARY KEY → recreación de tabla a `PRIMARY KEY(repo_id, path)`. Idempotencia por ESTRUCTURA (`column_exists` + rows-no-migradas), no por el stamp. Test de kill-post-COMMIT/pre-checkpoint agregado.
- [ ] Story 2: ¿cuándo promover `adaptive` de opt-in a default? — gate REFORZADO it.2: requiere (a) embedder code-trained (OQ-3 v0.5) Y (b) lift sostenido en el benchmark CodeSearchNet. La heurística es léxica-sin-señal-de-corpus, así que sola no puede flipear; revisar Decision B en release posterior.
- [ ] Story 1: pin exacto del release/split de CodeSearchNet + checksum para reproducibilidad cross-machine.
- [ ] Story 1 (it.2): Rust no tiene split en CodeSearchNet → queda cubierto por el slice ripgrep existente; el subset CSN es python+go+typescript (TS como first-class, N1). Revisar si aparece un corpus IR de Rust.
- [x] Story 3 (RESOLVED it.3, recomendación): `github-attestations-phase = "build-local-artifacts"` (default de cargo-dist + cadena SLSA más fuerte: cada plataforma atesta su binario en build time; el job build-local-artifacts ya corre, release.yml:90). `filters` default = todos. Re-tunear solo con medición en prerelease real. Hardening: el grep de verificación matchea `attest` genérico (cargo-dist migró a `actions/attest@v4`, commit 9324d73 2026-04-05), NO el literal `attest-build-provenance`.
- [x] Story 4 (RESOLVED it.3, blocker #1): el DB legacy pre-v0.7 para AC2/AC3 se construye con un HELPER EN CÓDIGO determinista (`build_legacy_pre_v07_db`), NO un `.db` commiteado (tests/fixtures/ solo tiene fuentes .rs/.ts/.go/.py + imports.*). AC0 nuevo: el builder existe y es determinista.
- [x] Story 4 (RESOLVED it.3, blocker #2 + #3): `repo_id = blake3(canonical(root)).to_hex()` (NO literal `'default'`; mismo esquema para el synthesized default de la migración legacy). Origen derivado internamente del `root` que reindex/index_repo YA reciben → CERO cambios de firma pública (migrate tiene 17 callers; reindex/index_repo ya llevan root). migrate(conn) hace la recreación estructural + backfill de placeholder; el primer reindex(root) reescribe placeholder→real hash. Tensión E1↔scoping resuelta: columna repo_id SIEMPRE escrita (forward-investment para E2), `WHERE repo_id=?` es defense-in-depth OPCIONAL, NO requisito de correctitud bajo E1 (aislamiento ya estructural por DB-por-repo).
- [ ] Story 4 (it.3, N6 declarado): un lost-update del registry sidecar es ACEPTABLE (mitigado por self-heal en el próximo reindex); NO se requiere read-modify-write bajo lock en v0.7. Escalar a RMW-bajo-lock solo si los lost-updates se vuelven costosos (ADR Follow-up #4). Único requisito duro: no-archivo-roto (atomic temp+rename).
- [ ] Story 4 (it.3): NO scopear `wipe_all` (incremental.rs:148) por repo_id — bajo E1 el `DELETE FROM files` global es correcto para force-reindex; el executor NO debe "arreglarlo".
